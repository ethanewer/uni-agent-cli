{
  "name": "cc",
  "version": "0.1.0",
  "description": "A fast cc CLI for switching between Claude Code, Codex, and Cursor Agent.",
  "type": "module",
  "bin": {
    "cc": "src/cc"
  },
  "files": [
    "src/cc",
    "src/cc.mjs",
    "src/pi-harness.mjs",
    "src/harness/**/*.mjs",
    "src/harness/**/*.md",
    "src/harnesses/**/*.py",
    "src/harnesses/**/*.txt",
    "scripts/postinstall.mjs",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall.mjs",
    "check": "node --check src/cc.mjs && node --check src/pi-harness.mjs && node --check scripts/postinstall.mjs && for f in src/harness/*.mjs src/harness/adapters/*.mjs; do node --check \"$f\" || exit 1; done && python3 -c \"import pathlib; files=list(pathlib.Path('src/harnesses').rglob('*.py'))+[pathlib.Path('tests/e2e_harness_bridges.py')]; [compile(path.read_text(), str(path), 'exec') for path in files]\"",
    "test": "npm run check && node tests/settings.test.mjs && node tests/permissions.test.mjs && node tests/harness_adapter.test.mjs && python3 -c \"compile(open('tests/fake_acp.py').read(), 'tests/fake_acp.py', 'exec')\" && bash tests/permission_auto_smoke.sh && bash tests/permission_persist_smoke.sh && bash tests/tui_smoke.sh"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@mariozechner/pi-tui": "^0.73.0"
  }
}
